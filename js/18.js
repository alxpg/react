// CONDICIONALES

    const disponible = 2000;
    const credito = 3000;

    if( disponible >= credito ) {
        console.log('Puedes pagar con tu credito');
    } else {
        console.log('No puedes pagar con tu credito');
    }   

    /**
     * > mayor que
     * < menor que
     * >= mayor o igual que
     * <= menor o igual que
     * == igual que
     * === igual que y mismo tipo de dato
     * != diferente de
     * !== diferente de y diferente tipo de dato
     * && and
     * || or
     * ! not
     */
//Las variables con let se puede reasignar el valor y se pueden inicializar sin valor
//Las variables con const no se pueden reasignar el valor y no se pueden inicializar sin valor

const contacto = "Cadena de string"; //a una variable constante no se le puede cambiar el valor reservado
//contacto = 2000;
console.log(contacto); // 2000

//const precio; //Las variables con const no se pueden declarar sin valor Tienen que tener un valor asignado
const precio = 3000;
console.log(precio); // 3000